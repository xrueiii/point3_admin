export type roomType = {
    displayName: string;
    email: string;
    date: string;
    span: string;
    phone: string;
    roomId: string;
}

export type roomInfoType = {
    roomId: string;
    roomName: string;
    roomContent: string;
}